export interface Service {
    id?: string;
    serviceName?: string;
    imageUrl?: string;
}

export interface ModelData {
    title: string;
}

export interface Metric {
    title: string;
    count: number;
}
